spring:
  application:
    name: ln-user-provider
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        group: LN_GROUP
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
        group: LN_GROUP
        name: ln-user-provider